apiVersion: apps/v1
kind: Deployment

metadata:
  name: frontend
  namespace: revhive

spec:
  replicas: 1

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
        - name: frontend
          image: krsourav14/frontend:latest

          ports:
            - containerPort: 80

          imagePullPolicy: Always
